// difficulty.js — AI difficulty multipliers (kept standalone to avoid circular imports
// between state.js and cities.js, both of which need it).
export const AI_DIFFICULTY_MULT = { easy: 0.8, normal: 1.0, hard: 1.3 };
